services:
  alloy:
    image: grafana/alloy:${ALLOY_TAG:-v1.10.2}
    volumes:
      - ./otel/src/alloy/config.alloy:/etc/alloy/config.alloy
    command:
      - run
      - /etc/alloy/config.alloy
      - --server.http.listen-addr=0.0.0.0:12345
    ports:
      - "12345:12345"
