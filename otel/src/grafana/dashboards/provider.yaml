apiVersion: 1

providers:
  - name: "RD" # A uniquely identifiable name for the provider
    folder: "RD" # The folder where to place the dashboards
    type: file
    options:
      path: "/etc/grafana/provisioning/dashboards/RD"
